package GeneralProblems.Arrays;

public class ArrayPermutations {
    // Check if two arrays are permutation of each other
    // Approach 1 - multiply all elements of array, add all elements of array - compare prd1 and prd2 && sum1 and sum2
    // Approach 1 - fails if an element is 0

    //Approach 2 - Sort arrays - compare elements

    //Approach 3 - hash map array 1 - do contains key of array 2 - if contains, remove from table, if not add to table.
    // At the end if table is notEmpty - not permutations.

}
